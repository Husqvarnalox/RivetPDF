# Rivet CI: portable foundation (PDFium-OFF).
#
# PDFium is intentionally NOT downloaded or built here: Rivet never fetches
# PDFium automatically, and a cacheable, maintainable CI strategy for a local
# static PDFium build is a separate later task. These jobs cover the
# PDFium-independent foundation: configure, build, ctest, zero warnings.
#
# The Ubuntu job is deliberately a GCC portability build (CMake's default
# /usr/bin/c++ is g++ on the runner): it validates the headers and sources
# against GCC/libstdc++, whose standard library does NOT provide the
# transitive includes Apple's libc++ does (this is what caught
# std::uint8_t-without-<cstdint> in src/core/Log.hpp). The macOS job
# validates the AppleClang/libc++ path the product actually ships with.
name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest   # GCC portability build (/usr/bin/c++ = g++)
          - os: macos-latest    # AppleClang product build
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install build tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build

      - name: Configure (PDFium OFF, warnings visible)
        run: |
          cmake -S . -B build -G Ninja \
                -DCMAKE_BUILD_TYPE=RelWithDebInfo \
                -DCMAKE_CXX_FLAGS="-Werror" \
                -DRIVET_BUILD_TESTS=ON

      - name: Build
        run: cmake --build build

      - name: Test
        run: ctest --test-dir build --output-on-failure
